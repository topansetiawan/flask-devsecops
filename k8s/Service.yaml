apiVersion: v1
kind: Service
metadata:
  name: flask-devsecops-service
spec:
  type: NodePort
  selector:
    app: flask-devsecops
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30001